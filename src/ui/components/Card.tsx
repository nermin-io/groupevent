import React from 'react';
import { styled } from '../stitches.config';
import Box from "./Box";

const Card = styled(Box, {
    background: 'white',
    borderRadius: 6,
    padding: 30
});

export default Card;